services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "5000:5000"
    environment:
      - MONGO_URI=mongodb+srv://phungquocquy23_db_user:77rxCcnhCW7BRUr0@cluster0.ftrmpdy.mongodb.net/test?retryWrites=true&w=majority

  frontend:
    build:
      context: ./cloud-lab/client
      dockerfile: Dockerfile
    ports:
      - "3000:3000"
    depends_on:
      - backend